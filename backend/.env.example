# Magda API Configuration
# Copy to .env and customize for local development

# Application settings
APP_NAME=Magda API
APP_ENV=development

# API configuration
API_V1_PREFIX=/api/v1

# CORS settings (comma-separated origins)
CORS_ORIGINS=http://localhost:5173,http://localhost:8080,http://127.0.0.1:5173,http://127.0.0.1:8080

# Supabase configuration (leave empty if not using Supabase locally)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
